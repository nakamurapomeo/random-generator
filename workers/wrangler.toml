name = "randgen-r2-worker"
main = "worker.js"
compatibility_date = "2024-01-01"

# R2 Bucket binding
[[r2_buckets]]
binding = "R2_BUCKET"
bucket_name = "randgen-backup"

# API Key for authentication (change this to your own secret key!)
[vars]
API_KEY = "randgen-secret-key-2026"
